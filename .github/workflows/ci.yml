# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   lint-python:
#     name: Python Lint
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: astral-sh/setup-uv@v3
#         with:
#           version: "latest"

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Lint Framework
#         working-directory: ckyclaw-framework
#         run: |
#           uv sync --extra dev
#           uv run ruff check .
#           uv run ruff format --check .

#       - name: Lint Backend
#         working-directory: backend
#         run: |
#           uv sync --extra dev
#           uv run ruff check .
#           uv run ruff format --check .

#   lint-frontend:
#     name: Frontend Lint
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 10

#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "pnpm"
#           cache-dependency-path: frontend/pnpm-lock.yaml

#       - name: Install & Lint
#         working-directory: frontend
#         run: |
#           pnpm install --frozen-lockfile
#           pnpm lint

#   test-python:
#     name: Python Tests
#     runs-on: ubuntu-latest
#     needs: lint-python
#     services:
#       postgres:
#         image: postgres:16-alpine
#         env:
#           POSTGRES_DB: ckyclaw_test
#           POSTGRES_USER: ckyclaw
#           POSTGRES_PASSWORD: test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd "pg_isready -U ckyclaw"
#           --health-interval 5s
#           --health-timeout 3s
#           --health-retries 5
#     steps:
#       - uses: actions/checkout@v4

#       - uses: astral-sh/setup-uv@v3
#         with:
#           version: "latest"

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Test Framework
#         working-directory: ckyclaw-framework
#         run: |
#           uv sync --extra dev
#           uv run pytest -v

#       - name: Test Backend
#         working-directory: backend
#         env:
#           CKYCLAW_DATABASE_URL: postgresql+asyncpg://ckyclaw:test@localhost:5432/ckyclaw_test
#         run: |
#           uv sync --extra dev
#           uv run pytest -v

#   test-frontend:
#     name: Frontend Tests
#     runs-on: ubuntu-latest
#     needs: lint-frontend
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 10

#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "pnpm"
#           cache-dependency-path: frontend/pnpm-lock.yaml

#       - name: Install & Test
#         working-directory: frontend
#         run: |
#           pnpm install --frozen-lockfile
#           pnpm test -- --run

#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     needs: [test-python, test-frontend]
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 10

#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "pnpm"
#           cache-dependency-path: frontend/pnpm-lock.yaml

#       - name: Build Frontend
#         working-directory: frontend
#         run: |
#           pnpm install --frozen-lockfile
#           pnpm build

#       - name: Build Docker Images
#         run: |
#           docker compose build backend frontend
