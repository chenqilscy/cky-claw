# CkyClaw Framework

Python Agent 运行时框架，为 CkyClaw 平台提供底层 Agent 执行能力。

## 安装

```bash
cd ckyclaw-framework
uv sync
```

## 开发

```bash
uv sync --extra dev
uv run pytest
uv run ruff check .
uv run mypy .
```
