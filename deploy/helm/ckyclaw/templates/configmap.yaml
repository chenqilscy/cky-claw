apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ckyclaw.fullname" . }}-backend
  labels:
    {{- include "ckyclaw.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
